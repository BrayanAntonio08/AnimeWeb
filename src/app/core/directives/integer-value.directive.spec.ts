import { IntegerValueDirective } from './integer-value.directive';

describe('IntegerValueDirective', () => {
  it('should create an instance', () => {
    const directive = new IntegerValueDirective();
    expect(directive).toBeTruthy();
  });
});
