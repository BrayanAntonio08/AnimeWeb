import { DecimalValueDirective } from './decimal-value.directive';

describe('DecimalValueDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalValueDirective();
    expect(directive).toBeTruthy();
  });
});
